Spring boot- framework- make use of setup all the apis and running the application 
JPA (Hibernate ) - dependency we use + MySql - database we use- configure db, schem
Lombok- tool use to reduce lot of boiler plate  codes
Intellij- any ide can use
Thymeleaf server side templating enigne + Bootstrap for styiling you application, connfigure bootstrap into the application and use
--------------------------------------------
1.List all apis / operations u would persform on  the project (CRUD operations)
2.spring initializer-
     dependencies-
	WEB- since it's a web application
	JPA-have to persist data into data storage ,make use of database MySQL 
	also need MySQL driver along with JPA-- to interact the application with the db then respective driver needed
	Lombok- its a library that make use of annotations to reduce boiler plate code
	Thymeleaf-